const {StyleSheet} = require('react-native');

const extStyle = StyleSheet.create({
  style: {
    fontSize: 30,
    color: 'green',
    backgroundColor: 'red',
    margin: 5,
    borderRadius: 10,
  },
});

export default extStyle;
